# NodeJS-Mongo Connection Example

Replace values as necessary

    export MONGOHQ_URL=mongodb://USERNAME:PASSWORD@SERVER:PORT/DB_NAME
    export PORT=8080
    npm install -g coffee-script
    npm install
    coffee server.coffee
